# erasure-bypass-scala
Trying to understand how to overcome type erasure in Scala using `ClassTag` and `TypeTag` using a generic methods that tries to filter out items of type `TMatch` given a `List[TNode]`
